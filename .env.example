# Copy this file to .env and fill in your own values before running `docker compose up -d`.

# The domain whose DNS A record points at this VPS. Caddy will automatically request and renew
# a Let's Encrypt certificate for it - the Android app's Proxy endpoint requires valid HTTPS.
PROXY_DOMAIN=proxy.example.com
